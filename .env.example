# -------------------------------------------------------------------
# 🌱 CORE
# -------------------------------------------------------------------
NODE_ENV=development

# -------------------------------------------------------------------
# 🌐 PUBLIC FLAGS (được client đọc)
# -------------------------------------------------------------------
NEXT_PUBLIC_USE_MOCK=0
NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_APP_ORIGIN=http://localhost:3000

# -------------------------------------------------------------------
# 🔐 AUTH (NextAuth + JWT)
# -------------------------------------------------------------------
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=change_me_nextauth_secret

# JWT — dùng cặp access/refresh thay vì 1 JWT_SECRET đơn
JWT_ACCESS_SECRET=change_me_access_secret_at_least_32_chars
JWT_REFRESH_SECRET=change_me_refresh_secret_at_least_32_chars

# -------------------------------------------------------------------
# 🗄 DATABASE
# -------------------------------------------------------------------
# Chọn 1 trong 2:
# 1️⃣ SQLite (dev/CI nhanh gọn)
DATABASE_URL="file:./dev.db"

# 2️⃣ PostgreSQL (dev qua docker-compose / production)
# DATABASE_URL="postgres://finzy:finzy@localhost:5432/finzy"

# -------------------------------------------------------------------
# ✉️ MAIL / SMTP
# -------------------------------------------------------------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_gmail@gmail.com
SMTP_PASS=your_app_password
SMTP_FROM="Finzy Support <your_gmail@gmail.com>"

# Gửi mail trong dev (0 = mock, 1 = gửi thật)
SEND_MAIL_ALWAYS=0

# -------------------------------------------------------------------
# 🔢 OTP CONFIG
# -------------------------------------------------------------------
OTP_TTL_SECONDS=300
OTP_RESEND_SECONDS=60

# -------------------------------------------------------------------
# 🧠 REDIS / BULLMQ
# -------------------------------------------------------------------
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
# Hoặc gọn bằng URL:
# REDIS_URL=redis://127.0.0.1:6379

# -------------------------------------------------------------------
# 🧑‍💻 DEV BYPASS (chỉ bật khi test)
# -------------------------------------------------------------------
DEV_AUTH_BYPASS=0
DEV_AUTH_USER_ID=1
DEV_AUTH_ROLE=ADMIN

# -------------------------------------------------------------------
# 🪶 LOGGING
# -------------------------------------------------------------------
LOG_DEBUG=1

